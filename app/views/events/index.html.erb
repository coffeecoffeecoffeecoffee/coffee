<% set_meta_tags title: 'Find an Event', reverse: true %>
<% set_meta_tags og: {
  title: 'Gather',
  type: 'website',
  image: request.base_url + '/apple-touch-icon.png',
  url: request.original_url
} %>

<%= render 'partials/event_cards', events: @events %>
