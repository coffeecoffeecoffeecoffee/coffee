<div class="event-cards">
  <% events.each do |event| %>
    <%= render 'partials/event_card', event: event %>
  <% end %>
  <%= 'There are no events scheduled. Check back later.' unless @events.count > 0 %>
</div>